```sh
npm init
npm install --save-dev typescript jest eslint
npx tsc --init
# Setup tsconfig.json file
# Setup webpack.config.json file
# Setup scripts build, build-tsc, build-webpack in package.json
npm run build  # Build
node build/index.js  # Run
npm init @eslint/config
# Copy .eslintrc.json from the MolStar project
```

